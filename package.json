{
  "name": "shirogane",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "worker-build --release",
    "build:dev": "worker-build --dev",
    "dev": "npm run build:dev && wrangler dev",
    "check": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && worker-build --dev",
    "test": "cargo test",
    "db:local": "wrangler d1 execute shirogane --local --file=./migrations/0001_init.sql && wrangler d1 execute shirogane --local --file=./migrations/0002_reliability.sql",
    "db:remote": "wrangler d1 execute shirogane --remote --file=./migrations/0001_init.sql && wrangler d1 execute shirogane --remote --file=./migrations/0002_reliability.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.114.0"
  }
}
